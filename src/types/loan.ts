export interface Loan {
  borrowAmount: string;
  collateral: string;
  prepaidDuration: number;
  projectId: number;
  terminal: string;
  token: string;
  chainId: number;
  createdAt: number;
  id: string;
}
